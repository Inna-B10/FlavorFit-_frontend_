query GetAllRecipes($input: RecipesQueryInput!) {
  allRecipes(input: $input) {
    calories
    cookingTime
    description
    difficulty
    dishType
    ingredientsVersion
    likesCount
    recipeId
    slug
    title
    userId
    user {
      avatarUrl
      firstName
    }
  }
}
