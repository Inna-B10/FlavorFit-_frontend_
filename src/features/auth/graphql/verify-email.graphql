mutation VerifyEmail($token: String!) {
  verifyEmail(token: $token) {
    accessToken
    user {
      avatarUrl
      email
      firstName
      role
      userId
      verificationToken
    }
  }
}
